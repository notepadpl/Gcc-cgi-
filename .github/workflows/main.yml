name: Compile emcc

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install emcc
      run: sudo apt-get install -y emscripten  
    - name: Install sdl2 
      run: sudo apt-get install -y libsdl2-dev libsdl2-2.0-0
    - name: Check emcc version
      run: emcc -v

    - name: Compile C to html
      run: emcc sdl2.c -s WASM=1 -s USE_SDL=2 -O3 -o index.html




    - name: Upload compiled Html
      uses: actions/upload-artifact@v3
      with:
        name: index.html
        path: ./index.html
